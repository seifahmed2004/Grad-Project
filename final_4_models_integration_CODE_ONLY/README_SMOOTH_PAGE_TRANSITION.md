# Smooth Page Transition Patch

Adds global vapor/fade/blur page transitions for all Streamlit pages, including login.

Apply:

```powershell
cd "C:\Users\lenovo\Downloads\final_4_models_integration_CODE_ONLY"
python .\smooth_page_transition_patch\apply_smooth_page_transition.py
```

Do not push if you want Streamlit Cloud to stay unchanged.
